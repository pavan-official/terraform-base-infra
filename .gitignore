# Local Terraform state
*.tfstate
*.tfstate.backup

# Crash logs
crash.log

# Terraform variables file containing sensitive data
*.tfvars
*.tfvars.json

# Terraform CLI configuration
.terraformrc
terraform.rc

# Local directories
.terraform/

# Override files (used for local changes that should not be shared)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive files for specific tools (e.g., AWS credentials, GCP keys)
# Example: Ignore AWS credentials if stored locally
.aws/
